let arregloNumeroUno: number[] = [1, 2, 3, 4, 5];
//declaracion de funciones
let sumarDosNumeros = (numeroUno: number, numeroDos: number): number => {
    return 2;
}
//forma rapida de crear funciones ahorrando lineas del this.
let potenciNUmero = numero => {
    return numero * numero;
}

let multiplicacionNumeros = (n1, n2) => n1 * n2;
let sumarotia = 0;
/*let resultadoForeach = arregloNumeroUno.forEach((valorDelarreglo, indice, arreglo) => {
    sumarotia += valorDelarreglo;
    console.log(valorDelarreglo);
    console.log(indice);
    console.log(arreglo);
});
//cerrar el FOreach
console.log('resultado Foreach', resultadoForeach);
console.log(sumarotia);*/
//el priimer parametro guarda un estado del reduce y el segundo es elvalor de el arreglo encada posiciion

/*let resultadoReduce = arregloNumeroUno.reduce((valorActualOperacion, valorDelarreglo) => {

    console.log(valorActualOperacion);
    console.log(valorDelarreglo);
    return valorActualOperacion+valorDelarreglo;
},0);*/


